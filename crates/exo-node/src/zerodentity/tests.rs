//! Integration tests for the 0dentity module.
//!
//! Module-level tests that exercise interactions between sub-modules.
//! Per-module unit tests live in each sub-module's `#[cfg(test)]` blocks.
//!
//! **Status: populated alongside subsequent PRs (APE-73+).**
